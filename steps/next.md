# Changes required as on 20-06-2026

- Remove ASCII art (too big)
- Prompt Caching (IMPORTANT)
- Implement `~/monkeyDcode/config.json` to store env variables/API Keys instead of storing them as plan texts
- Implement sqlite3 for session and context management (IMPORTANT)
- Outputs must be elaborate and viewers should be able to view wha's changed `git diff {filename}`
