Add TypeScript types to `src/cart.ts`.

The file is plain JavaScript (no type annotations). Add proper TypeScript types to all functions and data structures. Do not change the runtime behavior — only add types.
