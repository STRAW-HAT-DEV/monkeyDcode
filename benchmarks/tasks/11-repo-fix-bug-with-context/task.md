Users report that removing a todo from the list sometimes removes the wrong one.

Find and fix the bug in `src/store.ts`. Do not change the public API of `TodoStore` (`add`, `remove`, `list` must keep their current signatures) — `src/index.ts` and other callers depend on it.
