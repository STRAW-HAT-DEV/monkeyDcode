Implement `binarySearch` in `src/search.ts`.

The file is empty. Implement a binary search function that takes a sorted array of numbers and a target number, and returns the index of the target (or -1 if not found).

Must work in O(log n) time.
